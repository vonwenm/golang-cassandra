rm -rf target
mkdir -p target

if [ -n "$GO_PIPELINE_LABEL" ] ; then
    buildVersion=$GO_PIPELINE_LABEL
else
    buildVersion='LOCAL'
fi

sh scripts/apply-steps-to-local.sh &&
sh test &&
zip -rq target/casper-ml_$buildVersion.zip modules scripts
